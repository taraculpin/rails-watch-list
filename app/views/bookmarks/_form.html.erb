
<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment, label: 'Comment', error: 'Comment required' %>
  <%= f.association :movie, collection: Movie.all, label: 'Movie name', error: 'Movie title required' %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>
